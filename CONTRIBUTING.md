# Contributing

Install lerna

    npm i -g lerna

Install shared dev deps

    npm i

Install package deps and link them

    lerna bootstrap

Build packages

    lerna run build

Run tests

    lerna run test
